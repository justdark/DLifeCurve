# 人生曲线模拟器 PRD

> 基于飞书文档《人生曲线分析》（2026-04 版）的数学建模思想，落地为一款**面向终端用户的成熟 Web 产品**。用户首次进入完成简短的个人画像 onboarding，进入主页面看到属于自己的"基线人生曲线"；通过拖动事件参数，**即时**感受每个决策对人生总分的影响——这种"参数即反馈"的爽快感是产品的核心体验。

---

## 1. 产品定位

### 1.1 一句话定义
> 一款**面向个人**的人生决策可视化工具：先建立你的画像，再让你拖动每个人生事件的参数，实时看到对加权人生总分的影响。

### 1.2 产品调性
- **成熟感**：不是 demo / playground，是有完整流程（onboarding → 主页 → 设置 → 持久化）的产品
- **即时感**：拖动滑块的每一帧，曲线和总分都跟着动，反馈不延迟、不卡顿
- **个人感**：从第一秒开始就是"你的人生"，不是通用模板

### 1.3 用户与场景
| 用户 | 典型场景 |
|---|---|
| 25–45 岁、面临人生重大抉择的职场人 | "我该现在买房还是 5 年后？" "退休年龄怎么定？" |
| 对人生规划有结构化思考偏好的人 | 想用一条数学曲线代替直觉判断 |
| 决策对比者 | "工作到 55 vs 65 岁哪种总分高？" |
| 把玩型用户 | 拖着滑块感受不同选择的差异，像玩模拟人生 |

**不是**：
- 精确金融理财计算器（不替代专业建议）
- 命理/玄学工具
- 多人协同/社交产品

### 1.4 核心价值主张
1. **个人化**：进入即是"你的"曲线，不是别人的模板
2. **即时**：调一个参数，总分和四条曲线 < 16ms 内全部刷新
3. **可对比**：保存"方案 A/B/C"叠加显示，看清差异

### 1.5 非目标（v1 不做）
- 用户账号、云存储、多端同步（v1 数据全在本地）
- 移动端原生 App（先做响应式 Web）
- 蒙特卡洛多次采样（v1 用确定性期望值，v2 再加随机）
- AI 推荐"最优方案"（v3 才考虑）

---

## 2. 数学模型（核心，需精确实现）

### 2.1 目标函数
$$\mathcal{L} = \sum_{t=t_0}^{T} P_{t_0}(t) \cdot V(t)$$

其中：
- $t_0$：用户当前年龄（默认 32）
- $T$：寿命上限（取 100，因数据表上限为 "100岁及以上"）
- $P_{t_0}(t)$：在已活到 $t_0$ 的条件下，活到 $t$ 岁的条件生存概率
- $V(t)$：在 $t$ 岁那一年获得的体验值

### 2.2 三组核心方程

**(a) 生存概率**

设原始死亡分布 $N(t)$（来自 09-10 年统计死亡年龄表，"合计"列）：

$$d_0(t) = \frac{N(t)}{\sum_{i=t_0}^{T} N(i)} \quad \text{（在 } t_0 \text{ 已存活的条件下，死于 } t \text{ 岁的基础概率）}$$

事件 $j$ 对死亡分布的扰动 $\delta_{j}(t)$ 满足 $\sum_t \delta_j(t)=0$（重分布而非凭空增减）：

$$d(t) = \text{normalize}\left( d_0(t) + \sum_j \delta_j(t) \right)$$

$$P_{t_0}(t) = \sum_{i=t}^{T} d(i)$$

**(b) 财富迭代**

每年财富按"年初存量 → 年内净流量 → 年末计息"计算：

$$M(t+1) = \big(M(t) + \sum_j m_j(t)\big) \cdot (1 + r_\text{real})$$

约束：$M(t) + \sum_j m_j(t) \geq M_\text{floor}$（默认 0，允许用户开启"可借贷"）

其中 $r_\text{real} = r_\text{资产增值率} - r_\text{通胀率}$（默认 2%，可调）。

**(c) 体验值**

$$V(t) = f(t) \cdot \prod_j \mu_j(t) \cdot \big(1 + \sum_j \epsilon_j(t)\big)$$

- $f(t)$：年龄-体验能力系数（倒 U 形，board_3）
- $\mu_j(t)$：事件 $j$ 对体验"乘性"影响（如学习提升体验系数：1.05）
- $\epsilon_j(t)$：事件 $j$ 对体验"加性"贡献（基线"活着"= 0，使 $V \geq f(t) \cdot \prod \mu$）

**约束**：$\sum_j \tau_j(t) = 1$（时间归一化，归"活着"兜底）

### 2.3 三条内置常数曲线

| 曲线 | 形态 | 用途 |
|---|---|---|
| $f(t)$ 体验能力 | 倒 U：0 岁 0.5 → 30 岁 1.0 → 60 岁 0.75 → 80 岁 0.45 → 100 岁 0.25 | $V(t)$ 主乘子 |
| 工资曲线 $W(t)$ | 钟形：22 岁 12w → 35 岁 50w → 50 岁 55w → 60 岁 25w | 默认"工作"事件的收入 |
| 体验-金钱 $g(\$)$ | log 凹：温饱以下负，$10w/年 处饱和 | 消费类事件 $\epsilon$ 计算 |

> 这些曲线参数全部暴露在"高级设置"中可调，UI 默认值与 board_1/2/3 形态一致。

### 2.4 时间分配模型

每天 24h，归一化为 1.0：
- **睡觉** 默认 0.3333 (8h)，可调 0.25–0.42
- **活着（baseline）** = 1 - 睡觉 - Σ(其他事件时间占比)
- 其他事件（工作、学习、生娃带来的家庭时间…）从"活着"中扣除

如果 Σ 其他 > 1 - 睡觉，UI 报错"时间超载"。

### 2.5 数据健全性
- 死亡分布 CSV **表头与数据列不一致**（表头 `年龄,男,女,合计`，实际 `年龄,合计,男,女`，已用 32 岁/合计=男+女 验证）→ 加载时按位置而非按表头解析，并加单元测试
- 100 岁及以上单独一行，记为 t=100 的尾部桶
- 资产增值率/CPI 默认 4% / 2%；用户可输入区间，数值合法性 0–20%

---

## 3. 事件系统

### 3.1 事件接口（统一抽象）

```ts
interface LifeEvent {
  id: string                  // 唯一标识
  type: EventType             // 13 种内置类型 + custom
  name: string                // "结婚 with 张三"
  startAge: number            // 起始年龄（用户调）
  endAge: number | 'lifetime' // 结束年龄
  recurrence: 'once' | 'yearly' | 'lifetime'  // 时序模式

  // 影响系数（按年）
  timeShare: (age: number) => number     // τ_j(t)，0~1
  moneyFlow: (age: number) => number     // m_j(t)，单位元/年，可正可负
  expDelta:  (age: number) => number     // ε_j(t)，加性体验，可正可负
  expMult:   (age: number) => number     // μ_j(t)，乘性体验，默认 1
  deathShift: (age: number) => number    // δ_j(t)，对死亡分布的扰动

  // 用户可见参数
  params: Record<string, number | string>  // 事件类型决定的可调参数
}
```

### 3.2 13 类内置事件参数化

| # | 事件 | 关键参数（用户可调） | 时间 τ | 金钱 m（年） | 体验 ε / μ | 寿命扰动 δ |
|---|---|---|---|---|---|---|
| 0 | 活着 | 年生活成本 (5w 默认) | 兜底 | -living_cost | ε = 1 (基线) | – |
| 1 | 睡觉 | 睡眠时长 (8h) | 0.3333 | 0 | ε = log(h) 凹函数 | <6h: δ 增加 30+ 岁的死亡概率 |
| 2 | 吃饭 | 饮食水平（普通/精致） | 0.04 (1h) | -饮食年成本 | ε ∝ 等级 | 暴饮暴食缩寿，节制延寿 |
| 3 | 工作 | 行业、起止年龄、强度 (8/10/12h) | 0.33–0.5 | +W(t)·强度修正 | ε 取决于"工作满足度"参数 (-0.3~+0.5) | 强度高 δ 增 50+ 死亡 |
| 4 | 学习 | 起止年龄、年学费 | 0.05–0.2 | -学费 | μ = 1.02–1.10（永久乘性提升） | – |
| 5 | GAP | 起止年龄 | 0.66 | -生活成本，无收入 | ε = +0.3 | – |
| 6 | 结婚 | 年龄、配偶收入贡献、共同消费 | 0.05 | 共同消费均摊 | ε = +0.2~+1.0（幸福度参数） | δ < 0（统计上婚后延寿） |
| 7 | 生娃 | 年龄、个数、抚养年数 | 0.1/孩 | -养育费 (每年 5–20w/孩) | ε = +0.3~+0.8/孩，老年还有正向 | 老年 δ < 0（有子女陪伴） |
| 8 | 创业 | 年龄、初始投入、成功率、成功收益 | 0.5 | 期望值: 投入·(p·收益–1) | ε = +0.5 | – |
| 9 | 买房 | 年龄、总价、首付、贷款年限、利率 | 0 | 首付一次 + 房贷年供 | ε = +0.3 | – |
| 10 | 患重疾 | 年龄（默认随年龄概率分布触发）、治疗费 | 0.3 | -治疗费一次 + 后续年医疗 | ε = -0.5 | δ 大幅增加触发后 5 年内死亡 |
| 11 | 环球旅行 | 年龄、时长、总花费 | 临时 0.5 | -花费 | ε = +1.5（一次性大幅） | – |
| 12 | 买车 | 年龄、车价、年使用成本 | 0 | -车价 - 年使用 | ε = +0.2 | – |

> 表中"默认值"基于一线城市中产 2025 年水平，全部在事件配置面板可改。

### 3.3 基线场景生成（基于用户画像）

**v1 不再有"统一默认场景"，而是基于 onboarding 收集的用户画像，自动生成专属基线**。详见 §4.1 Onboarding。

举例：用户填了"34 岁/男/已婚/已有 1 孩/互联网/上海/初始资产 80w"，系统生成：
- 工作：23→62 岁，行业=互联网，强度=10h（基于"互联网"行业曲线）
- 结婚：32 岁起 lifetime（向当前年龄回推合理值）
- 生娃：33 岁，1 个孩，养至 22
- 买房：35 岁（推断为不久后），上海一线房价
- 退休：62 岁（法定）

加载主页面前**预跑一次**模拟器，确保用户进入即看到属于自己的曲线，而不是 loading。

---

## 4. 功能设计

### 4.1 Onboarding 流程（首次进入必经）

**目标**：30 秒内让用户完成画像并进入主页面，每一步都有"为什么问这个"的明示，降低填写抵触感。

**流程（4 步 + 加载）**：

```
[Step 1 欢迎]
  "你的人生，可以模拟"
  一句话 + 一张示例曲线动图
  [开始 →]
        ↓
[Step 2 基础画像]
  - 出生年份（默认当前-32）
  - 性别（男/女）
  - 所在城市（一线/新一线/二线/三四线）→ 影响房价、生活成本
  - 行业（IT/金融/制造/教育/公务员/自由职业/其他）→ 影响工资曲线
  - 当前总资产（万元，可大致估算）
  [上一步] [下一步 →]
        ↓
[Step 3 现状]
  - 婚姻：未婚/已婚/离异
  - 子女：0/1/2/3+
  - 是否有房：是/否
  - 健康自评：健康/亚健康/有慢性病
  [上一步] [下一步 →]
        ↓
[Step 4 偏好]（决定 ε/μ 系数）
  - 你享受工作吗？  滑块：很厌恶 ←——●——→ 很热爱
  - 婚姻幸福度？    滑块：糟糕 ←——●——→ 美满  （未婚禁用）
  - 你看重短期享受还是长期积累？  滑块：及时行乐 ←——●——→ 厚积薄发
  [上一步] [生成我的人生曲线 →]
        ↓
[加载页]
  "正在为你生成人生模型..."（500–1500ms 假动画 + 真模拟）
  小贴士轮播：解释 4 张曲线的含义
        ↓
[主页面]
```

**设计要点**：
- 全程**不强制邮箱/手机号注册**，纯本地（成熟感来自完成度，不来自账号墙）
- 每步顶部有"步骤进度条"
- 左下角始终有"跳过，使用默认画像"（兜底，但弱化）
- 完成后画像写入 localStorage，下次进入跳过 onboarding
- 任何字段都可在主页面"我的资料"入口里改

### 4.2 主页面布局

```
┌──────────────────────────────────────────────────────────────┐
│  [Logo]  人生曲线               [我的资料] [方案库] [设置] [?] │
├──────────────────────────────────────────────────────────────┤
│  ╔══════════════════════════════════════════════════════╗   │
│  ║  人生总分  78.3 / 100   (+2.1 vs 基线)               ║   │  ← 大字 + 实时差值
│  ║  期望寿命  84.5 岁  |  退休时财富  380w  |  最幸福  35–40 ║   │
│  ╚══════════════════════════════════════════════════════╝   │
├────────────────────────┬─────────────────────────────────────┤
│  事件时间线（甘特图）  │  四张曲线（2×2 网格）              │
│                        │                                     │
│ 工作 ████████████──    │  ┌─ 财富 M(t) ─┐  ┌─ 生存 P(t) ──┐ │
│ 结婚    ████████████   │  │             │  │              │ │
│ 生娃     ████████      │  └─────────────┘  └──────────────┘ │
│ 买房      ●            │  ┌─ 体验 V(t) ─┐  ┌─ 累积 L(t) ──┐ │
│ 退休            ●      │  │             │  │              │ │
│                        │  └─────────────┘  └──────────────┘ │
│ [+ 添加事件]           │                                     │
└────────────────────────┴─────────────────────────────────────┘
   ↑ 点击事件 → 右侧滑出"事件参数面板"（详见 §4.3）
```

### 4.3 核心交互：事件参数即时调节（产品的"心跳"）

> **这是整个产品最重要的交互。每一帧的延迟都是体验的损失。**

**触发链路**：

```
点击事件条
   ↓ 200ms slide-in
┌──── 右侧滑出参数抽屉（不遮挡曲线） ────┐
│  [工作 / 互联网]                  [×] │
│                                       │
│  起始年龄         23 ●─────────       │
│                  18    →   62         │
│  退休年龄                  62 ●───    │
│                          50    →  70  │
│  日均工作小时              10 ──●──   │
│                          6     →  14  │
│  工作满足度              中性 ───●─── │
│                                       │
│  ─────────────────────────────────    │
│  本事件对总分影响：  +12.4  ▲         │  ← 实时差值
│  对期望寿命：       -1.2 岁  ▼        │
│  对退休财富：       +180w  ▲          │
└───────────────────────────────────────┘
                   ↓ 用户拖动滑块
┌─ 拖动每一帧 (≤ 16ms) ─┐
│ 1. requestAnimationFrame 节流          │
│ 2. simulator(scenario) → SimResult     │
│ 3. 顶部总分数字 tween 动画 (200ms)     │
│ 4. 4 张曲线 morph 过渡 (200ms)         │
│ 5. 抽屉内"本事件影响"差值实时刷新      │
│ 6. 数字颜色随升降染色 (绿/红)          │
└────────────────────────────────────────┘
```

**情绪反馈细节**（"爽快感"由这些堆出来）：

| 反馈维度 | 实现 |
|---|---|
| 数字跳动 | 总分用 `<AnimatedNumber>` 200ms tween，不直接 setValue |
| 升降染色 | 上升 → 短暂闪绿（300ms）；下降 → 闪红 |
| 曲线形变 | ECharts 的 `animationDurationUpdate: 200`，配合贝塞尔过渡 |
| 触觉感 | 滑块拖到边界时有"吸附"感（步进 + 边界回弹动画 100ms）|
| 分段标记 | 关键年龄（退休/婚/孩）在曲线上有标记点，拖动时一起移动 |
| 实时差值 | "本事件影响：+12.4 ▲" 始终对比"未启用本事件"的反事实总分 |
| 失焦保留 | 拖动结束鼠标移开后，差值仍显示 3 秒，让用户消化 |
| 撤销 | Cmd+Z 撤销最后一次拖动（每次 mouseup 推一个 history） |

**反事实计算**（差值的语义）：
- 抽屉内"本事件影响"= L(当前完整 scenario) - L(scenario 移除本事件)
- 顶部"vs 基线"= L(当前) - L(初次进入主页时的画像基线)
- 两者并列展示，让用户既知道"这个事件值不值"，也知道"我现在比起点好多少"

### 4.4 其他主要交互

| 交互 | 触发 | 反馈 |
|---|---|---|
| 添加事件 | 点 [+ 添加事件] → 弹出 13 类卡片选择 | 选完直接进抽屉，预填合理默认值 |
| 删除事件 | 抽屉底部 [删除] | 二次确认，删除后曲线 morph |
| 时间线拖动事件起止 | 直接拖甘特条两端 | 即时模拟（同 §4.3 链路） |
| 整体平移事件 | 拖甘特条中段 | 即时模拟 |
| 保存方案 | 顶部 [保存当前为方案] | toast: "已保存为 方案 A"，进入方案库 |
| 对比方案 | 方案库勾选 2–3 个 | 曲线叠加，半透明，图例标注总分差 |
| 我的资料 | 顶部 [我的资料] | 抽屉打开 onboarding 表单，可改任意字段 |
| 重置 | 设置中 [清空数据重新开始] | 二次确认 → 回 onboarding |
| 导出 | 设置中 [导出 JSON] | 下载方案文件 + 整图 PNG |
| 撤销/重做 | Cmd+Z / Cmd+Shift+Z | 在最近 50 步内自由跳转 |

### 4.5 关键 UI 状态

| 状态 | 触发 | 设计 |
|---|---|---|
| 首次加载 | 无 localStorage 画像 | → onboarding |
| 已有画像加载 | 有 localStorage | 主页面骨架屏 50ms + 数据填入（不显示 loading） |
| 模拟中 | 拖动中 | < 16ms 不显示任何 loading 视觉，纯曲线动画 |
| 时间超载 | Σ τ > 1 | 抽屉对应字段标红 + 顶部红色 banner "你一天只有 24 小时"，但仍计算并显示（让用户感知冲突而非阻塞） |
| 财富透支 | M(t) < M_floor | 曲线在透支段染红 + 提示"X 岁起将入不敷出" |
| 空时间线 | 用户删光所有事件 | "添加你的第一个事件" CTA + 推荐卡片 |
| 错误兜底 | 模拟器抛错 | toast "计算异常" + 自动回滚到上一个有效状态 |

---

## 5. UI/UX 设计要点

### 5.1 视觉风格
- 整体克制、信息层级清晰，留白足够（成熟产品的标志）
- 配色：参考画板，黄/橙/紫三色分别对应"金钱/时间/体验"主题色；总分用主品牌色（深蓝/墨绿）
- 曲线美学：圆角粗笔触（呼应原画板手绘感），但坐标轴/网格清爽
- 字体：系统默认 + 等宽数字（`font-variant-numeric: tabular-nums`，避免数字跳动闪烁）
- 圆角：12–16px（卡片）/ 8px（按钮）；阴影克制（subtle elevation）

### 5.2 微交互规范（直接决定"成熟感"）

| 元素 | 规范 |
|---|---|
| 滑块拖动 | 拖把放大 1.1×；拖动时光环；松手回弹 100ms |
| 数字变化 | 始终用 tween 200ms ease-out，禁止直接 setText |
| 升降染色 | +↑：emerald-500 闪 300ms；-↓：rose-500 闪 300ms；后回归默认 |
| 曲线过渡 | ECharts `animationDurationUpdate: 200, animationEasingUpdate: 'cubicOut'` |
| 抽屉滑入 | 240ms cubic-bezier(0.4, 0, 0.2, 1) |
| Toast | 顶部居中，自动 3s 消失，可点击驻留 |
| Hover 反馈 | 所有可点击元素 100ms 内有视觉反馈（cursor / 颜色 / 轻微浮起）|
| 键盘导航 | 抽屉内 Tab 可遍历所有滑块，方向键调值 |

### 5.3 信息层次

- **第一眼**（5 秒）：onboarding 完成后，主页面正中是"你的总分 + 4 条曲线"，知道这是属于自己的工具
- **第二眼**（30 秒）：发现可以点事件、可以拖滑块，第一次拖动看到数字跳动 → "Aha"
- **深度使用**（5 分钟）：能保存方案、对比、调画像、撤销

### 5.4 关键文案

| 元素 | 文案示例 |
|---|---|
| 总分 | "人生总分 78.3 / 100" + 悬浮："E[Σ P(t)·V(t)] 归一化" |
| 实时差值 | "+2.1 vs 你的基线"（恒显示）|
| 抽屉差值 | "本事件对总分影响 +12.4 ▲"（移除该事件的反事实差值）|
| 期望寿命 | "你预期还可以活 52.5 年" |
| 曲线悬停 | "47 岁：财富 320w，生存 96%，体验 0.82" |
| 高亮峰值 | "你最幸福的 5 年：35–40 岁" |
| 时间冲突 | "你一天只有 24 小时，请减少其他事件" |
| 对比差异 | "方案 B 比方案 A 高 4.2 分；主要来源：晚 5 年退休 → 财富+80w" |

文案语气：**第二人称（"你"）**，避免"用户"、"该年龄段"等冷感词。

### 5.5 空态 / 错误态 / 引导

- **首次进入主页**：在曲线某一年龄打个 pulse 点 + 浮窗"试试拖一下'结婚'看看会怎样" → 用户拖一次后消失
- **拖到极端值**：滑块边界轻微震动 + tooltip "已是最大/最小"
- **localStorage 满**：toast 提示 + 提供导出 JSON 后清理
- **浏览器不支持**：底部 fallback 页提示"请用 Chrome/Safari/Edge 最新版"

### 5.6 响应式
- ≥ 1280px：左右分栏（时间线 + 曲线）+ 右侧抽屉
- 768–1279px：曲线压缩，时间线置顶 + 抽屉变 modal
- < 768px（v1 兜底）：仅展示总分 + 4 曲线 + 事件列表（无甘特图）；提示"建议大屏使用"

---

## 6. 技术方案

### 6.1 技术选型

| 层 | 技术 | 选型理由 |
|---|---|---|
| 框架 | React 18 + TypeScript + Vite | 主流、HMR 快、TS 强类型适合数学模型 |
| UI 组件 | shadcn/ui + Tailwind | 轻量、设计系统现成 |
| 图表 | Apache ECharts | 数据量小但需要联动 highlight、自定义曲线美学 |
| 状态 | Zustand | 比 Redux 轻、模型计算结果集中托管 |
| 数学库 | 自写（无重依赖） | 模型简单、可控、便于单测 |
| CSV | papaparse | 容错好 |
| 持久化 | localStorage（方案库） | v1 单机够用 |
| 部署 | Vercel/Cloudflare Pages | 静态站点 |
| 测试 | Vitest + Testing Library | 模型必须有单测 |

### 6.2 模块划分

```
src/
├── data/
│   ├── death-table.ts        # CSV 解析 + 修正表头错位
│   ├── salary-curve.ts       # 工资曲线常量（按行业）
│   ├── city-cost.ts          # 城市生活成本/房价常量
│   ├── exp-capacity.ts       # f(t) 倒U曲线
│   └── money-exp.ts          # g($) log凹函数
├── model/
│   ├── types.ts              # UserProfile / LifeEvent / SimResult 类型
│   ├── events/
│   │   ├── base.ts           # Event 基类
│   │   ├── work.ts
│   │   ├── marriage.ts
│   │   └── ...               # 13 个文件
│   ├── profile-to-scenario.ts # 用户画像 → 基线 Scenario 生成器
│   ├── simulator.ts          # 主迭代器：scenario → SimResult
│   ├── counterfactual.ts     # 移除某事件后的差值计算
│   └── simulator.test.ts     # 单测
├── store/
│   ├── profile.ts            # Zustand：UserProfile（持久化）
│   ├── scenario.ts           # 当前 scenario + 方案库（持久化）
│   ├── history.ts            # undo/redo 栈
│   └── ui.ts                 # 抽屉开关、当前选中事件等瞬态
├── hooks/
│   ├── useSimulation.ts      # scenario → SimResult，自动节流
│   ├── useCounterfactual.ts  # 实时差值
│   └── useHotkeys.ts         # Cmd+Z 等
├── pages/
│   ├── Onboarding/
│   │   ├── index.tsx         # 路由入口
│   │   ├── Step1Welcome.tsx
│   │   ├── Step2Basic.tsx
│   │   ├── Step3Status.tsx
│   │   ├── Step4Preference.tsx
│   │   └── LoadingTransition.tsx
│   └── Main/
│       ├── index.tsx
│       ├── ScoreHeader.tsx       # 顶部总分（含动画）
│       ├── EventTimeline.tsx     # 甘特图
│       ├── EventDrawer.tsx       # 右侧事件参数抽屉（核心）
│       ├── Charts/
│       │   ├── WealthChart.tsx
│       │   ├── SurvivalChart.tsx
│       │   ├── ExperienceChart.tsx
│       │   └── CumulativeChart.tsx
│       ├── ProfilePanel.tsx      # 我的资料（可改 onboarding 字段）
│       └── ScenarioLibrary.tsx   # 方案库 + 对比
├── components/
│   ├── AnimatedNumber.tsx        # 数字 tween + 升降染色
│   ├── DraggableSlider.tsx       # 自定义滑块（带边界回弹）
│   └── ...
├── lib/
│   ├── persist.ts                # localStorage 封装
│   └── tween.ts                  # 数字过渡动画
└── App.tsx
```

### 6.3 模拟器核心伪代码

```ts
function simulate(scenario: Scenario): SimResult {
  const ages = range(t0, T+1)
  const M = new Float64Array(ages.length)
  const P = new Float64Array(ages.length)
  const V = new Float64Array(ages.length)
  M[0] = scenario.initialWealth

  // 1. 计算扰动后的死亡分布 → P(t)
  const d = computeDeathDist(scenario.events, scenario.t0)
  for (let i = 0; i < ages.length; i++) {
    P[i] = sumFromIndex(d, i)
  }

  // 2. 按年迭代 V(t), M(t)
  for (let i = 0; i < ages.length; i++) {
    const t = ages[i]
    const activeEvents = scenario.events.filter(e => isActive(e, t))

    // 时间约束校验
    const totalTime = sumBy(activeEvents, e => e.timeShare(t))
    if (totalTime > 1) throw new TimeOverloadError(t, activeEvents)

    // 金钱流
    const flow = sumBy(activeEvents, e => e.moneyFlow(t))
    if (i > 0) M[i] = (M[i-1] + flow) * (1 + rReal)

    // 体验值
    const fT = expCapacity(t)
    const muProd = activeEvents.reduce((acc, e) => acc * e.expMult(t), 1)
    const epsSum = 1 + sumBy(activeEvents, e => e.expDelta(t))
    V[i] = fT * muProd * epsSum
  }

  // 3. 累积总分
  const L = sum(zip(P, V).map(([p, v]) => p * v))
  const cumL = cumsum(zip(P, V).map(([p, v]) => p * v))

  return { ages, M, P, V, L, cumL }
}
```

### 6.4 性能预算（硬指标，决定"成熟感"）
- 单次模拟：**< 5ms**（70 step、< 20 事件，目标 1ms）
- 反事实差值（移除某事件再算一次）：**< 5ms**
- 拖动滑块的端到端反馈（input → 总分更新 + 曲线重绘）：**< 16ms (60fps)**
- 首屏加载：< 1.5s（CSV 2KB、JS bundle < 300KB gzipped）
- onboarding 完成 → 主页面首屏：**< 800ms**（含模拟）

**关键优化手段**：

| 措施 | 理由 |
|---|---|
| 模拟器无依赖、纯函数、用 Float64Array | 避免 GC 抖动；可移到 Web Worker（v2） |
| 拖动事件用 `requestAnimationFrame` 节流到 1 帧 1 算 | 用户高频拖动时不会过载 |
| ECharts 用 `setOption(opt, { notMerge: false, lazyUpdate: true })` | 增量更新，不重新构建实例 |
| 数字动画用 `requestAnimationFrame` 自写 tween，不引入 framer-motion 全家桶 | 极简、可控 |
| Zustand 用 selector 订阅，避免不相关组件 re-render | 抽屉不应触发曲线重渲染 |
| 反事实差值用单独 worker 池（v1.1）| 主线程零阻塞 |
| 拖动开始时记 mark，松手后 perf.measure 上报（开发环境） | 可视化性能回归 |

### 6.5 路由

| 路径 | 页面 | 守卫 |
|---|---|---|
| `/` | 重定向：有 profile → `/app`，无 → `/onboarding` |
| `/onboarding` | Onboarding 4 步 | 完成后写 profile，跳 `/app` |
| `/app` | 主页面 | 无 profile 时反向跳 `/onboarding` |
| `/app/scenarios` | 方案库（v0.2） | – |

### 6.6 测试策略
- **单测**（必须）：
  - 死亡分布加载正确（32 岁那行：合计=男+女，14996=10571+4425）
  - $\sum_t d(t) = 1$（数值精度 < 1e-9）
  - 无事件时 V(t) = f(t)
  - 时间约束违反时抛错
  - 守恒：$M(T) = M(0) + \sum$ 净流量（去掉利息后）
  - 反事实：simulate(s) - simulate(s 移除事件 j) 与抽屉显示的差值一致
  - profile-to-scenario：3 种典型画像生成的事件数 / 财富量级合理
- **集成测试**：默认画像下总分稳定（回归保护）
- **快照测试**：4 张曲线 SVG 不无故抖动
- **E2E**（v0.3+）：onboarding → 拖动 → 保存方案 全链路 Playwright

---

## 7. 数据结构

### 7.1 用户画像（Onboarding 产出）

```ts
type Gender = 'male' | 'female'
type City = 'tier1' | 'tier1.5' | 'tier2' | 'tier3'   // 一线/新一线/二线/三四线
type Industry = 'it' | 'finance' | 'manufacturing' | 'education' | 'civil-service' | 'freelance' | 'other'
type Marriage = 'single' | 'married' | 'divorced'
type Health = 'healthy' | 'sub-healthy' | 'chronic'

interface UserProfile {
  // Step 2 基础
  birthYear: number
  gender: Gender
  city: City
  industry: Industry
  initialWealth: number          // 万元

  // Step 3 现状
  marriage: Marriage
  childrenCount: 0 | 1 | 2 | 3
  hasHouse: boolean
  health: Health

  // Step 4 偏好（决定事件 ε/μ）
  workSatisfaction: number       // -1 ~ +1（厌恶 ↔ 热爱）
  marriageHappiness: number      // 0 ~ 1（糟糕 ↔ 美满）
  timePreference: number         // -1 ~ +1（及时行乐 ↔ 厚积薄发）

  // 元信息
  completedAt: number            // 时间戳
  version: number                // schema 版本，未来迁移用
}
```

### 7.2 全局参数 / 场景 / 模拟结果

```ts
interface GlobalParams {
  currentAge: number          // t_0，从 birthYear 计算
  initialWealth: number       // 元
  gender: Gender
  realReturn: number          // r_real, 默认 0.02
  livingCost: number          // 年生活成本基线（由 city 决定）
}

interface Scenario {
  id: string
  name: string                // "我的人生" / "方案 A"
  isBaseline: boolean         // onboarding 后生成的初始方案
  globalParams: GlobalParams
  events: LifeEvent[]
  createdAt: number
  updatedAt: number
}

interface SimResult {
  ages: number[]              // [t_0, t_0+1, ..., 100]
  M: number[]                 // 财富
  P: number[]                 // 生存概率
  V: number[]                 // 单年体验
  EV: number[]                // P(t)·V(t)，期望体验
  cumL: number[]              // 累积人生分
  L: number                   // 总分（原始）
  Lnorm: number               // 归一化到 0-100
  expectedLifespan: number    // E[剩余寿命]
  peakYears: [number, number] // 最幸福 5 年区间
  warnings: SimWarning[]      // 时间超载、财富透支等
}

interface SimWarning {
  type: 'time-overload' | 'wealth-depleted' | 'lifetime-too-short'
  age: number
  message: string
  affectedEventIds?: string[]
}
```

### 7.3 持久化（localStorage）

```ts
// key: "lifecurve.v1"
{
  "version": 1,
  "profile": { ... UserProfile ... },           // 缺失则跳 onboarding
  "currentScenarioId": "abc",
  "scenarios": [
    { "id": "abc", "name": "我的人生", "isBaseline": true, ... },
    { "id": "def", "name": "方案 A（晚退休）", ... }
  ],
  "advancedSettings": {                          // 用户改了内置曲线才有
    "expCapacityCurve": [...] | null,
    "salaryCurveOverride": [...] | null
  },
  "uiState": {                                   // 不参与模拟的偏好
    "lastDrawerWidth": 360,
    "hasSeenDragHint": true
  }
}
```

### 7.4 撤销栈

```ts
interface HistoryEntry {
  scenarioSnapshot: Scenario   // 深拷贝
  timestamp: number
  description: string          // "调整工作起始年龄 22 → 24"
}

// 维持最多 50 条，超出 LRU
```

---

## 8. MVP 范围与 Roadmap

> **MVP 必须包含完整的"成熟感闭环"**：onboarding + 实时拖动反馈 + 持久化 + 撤销。少了任意一项都不算 MVP。

### 8.1 MVP（v0.1，目标 1.5 周）

**Onboarding & 进入**
- ✅ 4 步 onboarding 流程（基础/现状/偏好 + 加载页）
- ✅ 画像 → 基线 scenario 自动生成
- ✅ 主页面 + 完整路由守卫
- ✅ 我的资料入口（可改 onboarding 字段）

**模型 & 数据**
- ✅ 死亡分布 CSV 加载 + 修正（按位置解析）
- ✅ 工资曲线（按行业 4 类）+ 城市生活成本（4 档）
- ✅ f(t) / g($) 内置曲线
- ✅ 单测覆盖核心方程

**核心交互**
- ✅ 6 类核心事件：工作、结婚、生娃、买房、退休、环球旅行
- ✅ 事件时间线（甘特图）+ 起止拖动
- ✅ **事件参数抽屉**（产品心跳）：滑块拖动实时反馈，差值显示，60fps
- ✅ 4 张曲线（财富/生存/体验/累积）+ 总分顶部栏（含动画）
- ✅ AnimatedNumber + 升降染色
- ✅ 撤销/重做（Cmd+Z）

**持久化**
- ✅ Profile / 单方案 写入 localStorage
- ✅ 刷新页面恢复

**质量保证**
- ✅ 单测：模拟器 + 反事实 + 数据加载
- ✅ 性能：拖动稳定 60fps（实测）
- ✅ 中文界面 + 移动端兜底（曲线-only 视图）

### 8.2 v0.2（+ 1 周）
- 剩余 7 类事件（睡觉、吃饭、学习、GAP、创业、患重疾、买车）
- 方案库 + 多方案对比（最多 3 个，曲线叠加）
- 时间线甘特图的细节优化（hover 高亮、拖动吸附）
- 高级设置（自定义 f(t)、工资曲线，可视化编辑）

### 8.3 v0.3
- 导入/导出 JSON 方案
- 关键洞察生成器："最幸福 5 年""退休财富""人生分主要来源"
- 引导教程（首次拖动 hint、曲线含义提示）

### 8.4 v1.0
- 蒙特卡洛随机事件（重疾、意外、市场波动）
- 概率区间显示（95% CI 阴影带）
- 模拟移到 Web Worker，主线程纯 UI

### 8.5 v2.0（探索）
- AI 推荐"局部最优"参数（梯度上升或贝叶斯优化）
- 用户账号 + 云同步
- 公开方案分享 / 排行榜（去标识化的总分对比）

---

## 9. 边界情况与风险

### 9.1 模型边界
| 场景 | 处理 |
|---|---|
| 用户当前年龄 < 18 或 > 80 | 警告但允许；t_0 < 18 时，工作起始默认上调到 18 |
| 用户初始财富为负 | 允许（学贷场景），但 M_floor 调整 |
| 事件起止年龄反了 | 校验：startAge ≤ endAge，否则禁止 |
| 死亡前还有未发生事件 | 模拟器忽略 t > 实际死亡的事件（但用期望值不会"实际死亡"，全程算到 T=100） |
| 通胀率 > 收益率 | 允许，财富会缩水，曲线会下降 |
| 100 岁及以上 | 全部并入 t=100 桶 |

### 9.2 心理风险
- **不引发焦虑**：UI 文案避免"你大概率 80 岁就会死"这类直白措辞，改用"你预期还可以活 52.5 年"
- **不构成建议**：底部固定免责声明"本工具基于统计建模，仅作思考辅助，不构成任何投资/医疗/婚恋建议"
- **不收集隐私**：所有数据仅在本地浏览器，不上传

### 9.3 实现风险
| 风险 | 缓解 |
|---|---|
| 数学模型有 bug 导致曲线诡异 | 强制单测覆盖核心方程 + 默认场景回归测试 |
| 事件配置 UI 复杂度爆炸 | v1 限定 5 个核心事件，v2 再扩展 |
| 死亡分布 CSV 表头错位 | 加载层固化按位置解析，加单测断言 32 岁=14996 |
| 工资/体验曲线参数主观性 | 全部暴露为可调，并标注"默认值参考来源" |
| 计算性能 | 70 step × 20 事件 = 1400 次基础运算，毫秒级，不会是问题 |

### 9.4 待补数据/调研
- [ ] 中国分性别死亡分布（CSV 已有 男/女 列，但其分布与"合计"不同，性别参数生效后要切换）
- [ ] 各行业年龄-工资曲线（v1 用一条 IT 行业曲线兜底）
- [ ] 重疾年龄触发概率分布（v1 用固定年龄，v2 改概率）
- [ ] 婚姻幸福度的统计分布（学术文献有，先用文档建议的"用户自评"）

---

## 10. 开放问题（需用户确认后再开工）

### 10.1 产品决策
1. **MVP 事件数**：6 个核心事件（工作/结婚/生娃/买房/退休/环球旅行）够不够？想砍到更少（提前出 demo）还是要加？
2. **总分展示**：建议 **0–100 归一化分**（更直观），同时悬浮显示原始 $\mathcal{L}$。OK 吗？
3. **死亡描述**：默认 **"预期还可以活 X 年"**；高级折叠区显示 50% / 90% 寿命点。OK 吗？
4. **基线方案命名**：onboarding 完成后生成的方案默认叫"我的人生"，可改为"基线方案"或自定义？
5. **第一次拖动引导**：在主页面打 pulse 点引导用户拖一下（默认开启），用户拖完一次后永久关闭。是否需要？

### 10.2 技术 / 实现

6. **图表库**：ECharts（默认推荐，自定义性强、动画好）；或者 Recharts / Visx / D3。你有偏好吗？
7. **UI 组件库**：shadcn/ui + Tailwind（现代轻量）；或者 Ant Design / Arco（中文产品成熟选择）。你倾向？
8. **撤销栈深度**：50 步够吗？
9. **是否需要"匿名上报"**：v1 不做。v2 是否考虑做（仅总分 + 画像分桶，用于改进默认参数）？

### 10.3 数据 / 模型

10. **配偶 / 子女 财富贡献**：v1 简化为"共同消费均摊"参数，还是建模到事件维度（配偶有自己的工资曲线，子女抚养费分阶段）？
11. **行业工资曲线**：v1 建议给 IT/金融/制造/教育/公务员/自由职业/其他 这 7 档，每档一条曲线。OK？
12. **风险提示**：底部固定免责声明 "本工具基于统计建模，仅作思考辅助，不构成任何投资/医疗/婚恋建议"。需要更严肃化吗？

---

我倾向的默认答案：**1=6 个、2=归一化分、3=只显示期望、4=我的人生、5=需要、6=ECharts、7=shadcn/ui、8=50 够、9=v1 不做、10=简化均摊、11=7 档、12=保持当前措辞**。

如果你都同意可以直接说"按你的默认走"，我就开始 init 项目；不同意的我们在那条上对齐再开工。

---

## 附：关键参考

- 飞书原文档：[人生曲线分析](https://worldbuilder.feishu.cn/wiki/SwETwJ62BijCinkqcTOcZtbqnBf)
- 死亡数据：`09-10年统计死亡年龄.csv`（项目根目录，需修正表头）
- 三张概念曲线：`/tmp/feishu_life_curve_assets/board_{1,2,3}.png`
